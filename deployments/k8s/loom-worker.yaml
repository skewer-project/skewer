apiVersion: apps/v1
kind: Deployment
metadata:
  name: loom-worker
  namespace: default
  labels:
    app: loom-worker
spec:
  replicas: 0 # Scaled dynamically by the Coordinator
  selector:
    matchLabels:
      app: loom-worker
  template:
    metadata:
      labels:
        app: loom-worker
    spec:
      containers:
        - name: worker
          # NOTE: Replace this with Google Artifact Registry URI
          image: loom-worker:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: COORDINATOR_ADDR
              value: "skewer-coordinator.default.svc.cluster.local:50051"
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: "/etc/secrets/credentials.json"
          volumeMounts:
            - name: gcp-credentials
              mountPath: "/etc/secrets"
              readOnly: true
            - name: skewer-data
              mountPath: "/data"
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "2000m"
              memory: "4Gi"

      volumes:
        - name: gcp-credentials
          secret:
            secretName: skewer-gcp-creds
            optional: true
        - name: skewer-data
          hostPath:
            path: "{{SKEWER_DATA_PATH}}"
            type: DirectoryOrCreate
